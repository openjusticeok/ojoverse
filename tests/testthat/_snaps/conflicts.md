# useful conflicts message

    Code
      ojoverse_conflicts(c("base", "stats"))
    Output
      -- Conflicts ------------------------------------------- ojoverse_conflicts() --
      x ::() masks 
      

